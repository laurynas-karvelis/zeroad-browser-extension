{
  "name": "@zeroad.network/browser-extension",
  "version": "0.9.3",
  "license": "Apache-2.0",
  "repository": "github:laurynas-karvelis/zeroad-browser-extension",
  "homepage": "https://zeroad.network",
  "author": {
    "name": "Laurynas Karvelis <Explosive Brains Ltd>",
    "email": "hello@zeroad.network",
    "url": "http://zeroad.network"
  },
  "type": "module",
  "scripts": {
    "manifest:build": "bun run ./scripts/build-manifest.ts",
    "popup:build": "bun run ./scripts/build-popup.ts",
    "assets:build": "rsync -a --delete src/images/ build/images/ && rsync -a --delete src/fonts/ build/fonts/",
    "styles:build": "sass --no-charset --no-source-map --quiet --style=compressed --load-path=node_modules src/styles/main.scss build/css/main.css && bun run ./scripts/purge-css.ts",
    "scripts:build": "bun build src/*.ts --outdir=build/js/ --target=browser",
    "targets:build": "dirs=(\"chrome\" \"firefox\"); for d in \"${dirs[@]}\"; do (rsync -a --delete --mkpath build/ targets/$d/ && bun manifest:build $d > targets/$d/manifest.json); done",
    "watch": "bun run ./scripts/watch.ts",
    "test": "bun test --isolate",
    "build": "bun scripts:build && bun assets:build && bun styles:build && bun popup:build && bun targets:build",
    "postbuild": "dirs=(\"chrome\"); for d in \"${dirs[@]}\"; do (cd \"targets/$d\" && rm js/content.js); done",
    "package": "version=$(jq -r '.version' ./package.json) dirs=(\"chrome\" \"firefox\"); for d in \"${dirs[@]}\"; do (cd \"targets/$d\" && zip -r $d-$version.zip manifest.json js popup.html css images fonts); done"
  },
  "devDependencies": {
    "@styles": "workspace:*",
    "@types/chrome": "^0.2.9",
    "@types/firefox-webext-browser": "^143.0.0",
    "chokidar": "^5.0.0",
    "happy-dom": "^20.14.0",
    "hono": "^4.13.7",
    "json-e": "^4.8.4",
    "lucide": "^1.44.0",
    "sass": "^1.104.0"
  }
}
